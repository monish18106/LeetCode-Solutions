class Solution:
    def minimumDifference(self, nums: List[int], k: int) -> int:
        n = len(nums)
        nums.sort()
        m = 10**9
        for i in range(n-k+1):
            m = min(m,nums[i+k-1] - nums[i])
        return m

        